import { NotificationsPageBody } from "@/app/(notifications)/notifications-page";

export const metadata = { title: "Notifications" };
export default async function Page() {
  return <NotificationsPageBody />;
}
